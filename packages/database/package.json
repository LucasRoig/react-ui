{
  "name": "@repo/database",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push-dev": "prisma migrate dev",
    "db:push-dev-create-only": "prisma migrate dev --create-only",
    "db:format": "prisma format",
    "db:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "drizzle-orm": "^0.41.0"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "prisma": "^6.6.0",
    "prisma-generator-drizzle": "^0.7.6"
  },
  "volta": {
    "node": "22.12.0"
  }
}
